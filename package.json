{
  "name": "keigo-counter",
  "version": "1.0.0",
  "private": true,
  "description": "敬語禁止交流会の点数表（ルーム共有カウンター）",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\"",
    "dev": "node test/dev-server.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
